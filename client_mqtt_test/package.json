{
  "name": "client_mqtt_test",
  "version": "1.0.0",
  "description": "Test client for data flow rabbit mq -> influx db -> grafana",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "mqtt": "^4.2.8"
  }
}
